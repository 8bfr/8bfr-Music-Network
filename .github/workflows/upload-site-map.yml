name: 🟣 Upload 8BFR Site Map SVG

on:
  workflow_dispatch:

jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      - name: 🧩 Checkout repo
        uses: actions/checkout@v4

      - name: 🖋 Create /assets/8bfr-site-map.svg
        run: |
          mkdir -p assets
          cat <<'EOF' > assets/8bfr-site-map.svg
          <svg xmlns="http://www.w3.org/2000/svg" width="800" height="600" viewBox="0 0 800 600">
            <rect width="800" height="600" fill="#0a001a"/>
            <text x="400" y="80" font-family="Poppins" font-size="32" fill="#a020f0" text-anchor="middle">
              8BFR Music Network
            </text>
            <circle cx="400" cy="300" r="150" stroke="#a020f0" stroke-width="4" fill="none">
              <animate attributeName="opacity" values="1;0.3;1" dur="2s" repeatCount="indefinite"/>
            </circle>
            <text x="400" y="310" font-family="Poppins" font-size="18" fill="#fff" text-anchor="middle">
              Create • Connect • Collab
            </text>
          </svg>
          EOF

      - name: 🚀 Commit and push SVG
        run: |
          git config --global user.name "8BFR Bot"
          git config --global user.email "records.8bfr.music.network@gmail.com"
          git add assets/8bfr-site-map.svg
          git commit -m "🟣 Added 8BFR site map SVG"
          git push https://x-access-token:${{ secrets.UPLOAD_TOKEN }}@github.com/8bfr/8bfr-Music-Network.git HEAD:main
